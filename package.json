{
  "name": "hdr_video_viewer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 8000",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.2"
  }
}
